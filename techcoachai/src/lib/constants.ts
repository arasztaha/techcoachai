export const API_BASE_URL = 'http://localhost:5000'; // Updated to point to the in-memory backend
export const AUTH_API_URL = `${API_BASE_URL}/api/auth`;

// Add a flag to indicate whether we're using a mock backend for development
export const MOCK_AUTH = true;
